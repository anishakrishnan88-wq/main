_schema-version: 3.3.0
ID: IMP_XSUAA_AUTH
description: A simple CAP project.
version: 1.0.0
modules:
  - name: IMP_XSUAA_AUTH-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: IMP_XSUAA_AUTH-db
      - name: uaa_IMP_XSUAA_AUTH
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      instances: 1
    build-parameters:
      builder: npm-ci
  - name: IMP_XSUAA_AUTH-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: IMP_XSUAA_AUTH-db
    parameters:
      buildpack: nodejs_buildpack
  - name: IMP_XSUAA_AUTH-destination-content
    type: com.sap.application.content
    requires:
      - name: IMP_XSUAA_AUTH-destination-service
        parameters:
          content-target: true
      - name: IMP_XSUAA_AUTH_html_repo_host
        parameters:
          service-key:
            name: IMP_XSUAA_AUTH_html_repo_host-key
      - name: uaa_IMP_XSUAA_AUTH
        parameters:
          service-key:
            name: uaa_IMP_XSUAA_AUTH-key
    parameters:
      content:
        instance:
          destinations:
            - Name: Fiori_IMP_XSUAA_AUTH_html_repo_host
              ServiceInstanceName: IMP_XSUAA_AUTH-html5-app-host-service
              ServiceKeyName: IMP_XSUAA_AUTH_html_repo_host-key
              sap.cloud.service: Fiori
            - Authentication: OAuth2UserTokenExchange
              Name: Fiori_uaa_IMP_XSUAA_AUTH
              ServiceInstanceName: IMP_XSUAA_AUTH-xsuaa-service
              ServiceKeyName: uaa_IMP_XSUAA_AUTH-key
              sap.cloud.service: Fiori
          existing_destinations_policy: ignore
    build-parameters:
      no-source: true
  - name: IMP_XSUAA_AUTH-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: IMP_XSUAA_AUTH_html_repo_host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - fiori.zip
          name: fiori
          target-path: resources/
  - name: fiori
    type: html5
    path: app/fiori
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
resources:
  - name: IMP_XSUAA_AUTH-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: IMP_XSUAA_AUTH-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
              - Authentication: NoAuthentication
                HTML5.DynamicDestination: true
                HTML5.ForwardAuthToken: true
                Name: srv-api
                ProxyType: Internet
                Type: HTTP
                URL: ~{srv-api/srv-url}
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: IMP_XSUAA_AUTH-destination-service
      service-plan: lite
    requires:
      - name: srv-api
  - name: IMP_XSUAA_AUTH_html_repo_host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: IMP_XSUAA_AUTH-html5-app-host-service
      service-plan: app-host
  - name: uaa_IMP_XSUAA_AUTH
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-name: IMP_XSUAA_AUTH-xsuaa-service
      service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
